# Copy to .env for local testing (GitHub Actions reads from repository secrets instead)

URLSCAN_API_KEY=
ABUSEIPDB_API_KEY=
GSB_API_KEY=
ANTHROPIC_API_KEY=

# Only needed for PowerShell / FastAPI (not used inside GHA itself)
GH_DISPATCH_TOKEN=
GH_REPO_OWNER=
GH_REPO_NAME=
